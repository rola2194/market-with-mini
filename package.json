{
  "dependencies": {
    "firebase": "^11.6.0"
  },
  "devDependencies": {
    "parcel": "^2.14.4",
    "process": "^0.11.10"
  },
  "scripts": {
    "build": "parcel build index.html --no-scope-hoist --public-url ./ --dist-dir docs"
  }
}
